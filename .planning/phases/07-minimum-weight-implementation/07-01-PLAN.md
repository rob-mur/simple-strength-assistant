---
wave: 1
depends_on: []
files_modified:
  - src/components/exercise_form.rs
  - src/state/workout_state.rs
  - tests/steps/library_steps.rs
  - tests/steps/workout_steps.rs
  - tests/steps/exercise_search_steps.rs
  - tests/steps/exercise_list_steps.rs
  - src/models/validation.rs
  - src/models/exercise.rs
autonomous: true
---

# Plan 07-01: Configuration and UI Updates for Minimum Weight

## Objective
Update the Exercise model's default configurations and the UI to rename "Starting Weight" to "Minimum Weight" and default the value to 0, satisfying CONF-01, CONF-02, and CONF-03. Update corresponding BDD and unit tests.

## Tasks

<tasks>
  <task id="7-01-01" autonomous="true">
    <description>Update `src/components/exercise_form.rs` to replace "Starting Weight (kg)" with "Minimum Weight (kg)" in the UI.</description>
    <requirements>CONF-03</requirements>
    <instructions>
      1. Open `src/components/exercise_form.rs`.
      2. Find the label containing "Starting Weight (kg)" and change it to "Minimum Weight (kg)".
      3. Do not change the underlying variable name `min_weight`.
    </instructions>
  </task>

  <task id="7-01-02" autonomous="true">
    <description>Change default `min_weight` from 45.0 to 0.0 in `src/components/exercise_form.rs` and update tests.</description>
    <requirements>CONF-02, CONF-01</requirements>
    <instructions>
      1. In `src/components/exercise_form.rs`, update the fallback default value for `min_weight` from 45.0 to 0.0.
      2. In `tests/steps/library_steps.rs`, update any assertions checking for default 45.0 to 0.0, or step definitions related to initial minimum weight.
      3. Update `src/state/workout_state.rs` tests (e.g., `test_initial_predictions_weighted`) to use a 0.0 default if necessary, or update assertions if the default changes.
    </instructions>
  </task>

  <task id="7-01-03" autonomous="true">
    <description>Update `tests/steps/workout_steps.rs` and other test files to use 0.0 as default min weight and fix failing tests.</description>
    <requirements>CONF-02</requirements>
    <instructions>
      1. In `tests/steps/workout_steps.rs`, change `min_weight: 45.0` to `0.0`.
      2. In `tests/steps/exercise_search_steps.rs` and `tests/steps/exercise_list_steps.rs`, ensure default `min_weight` is consistent.
      3. Run `cargo test` and fix any remaining instances of `45.0` used as a default minimum weight in unit tests.
    </instructions>
  </task>
</tasks>

## Verification
- [ ] UI displays "Minimum Weight (kg)" instead of "Starting Weight (kg)".
- [ ] New exercises default to 0 kg minimum weight.
- [ ] `cargo test` passes.
- [ ] `./scripts/ci-test.sh` passes (E2E and BDD tests).

## must_haves
- No instance of "Starting Weight" should exist in the codebase.
- Minimum weight must default to 0.
